---
title: 빅분기 3유형 암기사항 모음
categories: [05_통계]
---

## 0. 기초 통계량

### 0_1.신뢰구간 구하기

```python 
from scipy.stats import t
t.interval(0.95,df,mu,sem)
```

## 1. 정규성 검정

```python
from scipy.stats import shapiro

stat, p_val = shapiro(data)
```
## 2. T 검정
### 1.단일 표본 검정 from scipy.stats import ttest_1samp

```python
stat, p_val = ttest_1samp(data, mu, alternative='')
```

### 2. 독립 표본 ttest

```python
from scipy.stats import ttest_ind

stat, p_val = ttest_ind(group1, group2)

```

### 2. 대응표본 ttest
``` python
from scipy.stats import ttest_rel

stat, p_val = ttest.rel(group1, group2, alternative ='')
```

## 3. Anova





### 일원배치법

```python

import scipy.stats import f_oneway
f_value, p_value = f_oneway(groupA, groupB, groupC)
```


### 쿠르스칼 왈리스 검정

```python
from scipy.stats import kruskal

s,p = kruskal(a,b,c) #a,b,c집단
```


## 4. 상관계수
```python
df.corr()
```

## 5. 회귀모형
``` python
import statsmodels.api as sm

x.add_constant(x)
model = sm.OLS(y,x)
res = model.fit()
```

> 출력 결과 확인
``` python
res.summary()
res.params
res.pvalues
```


## 6. 로지스틱 회귀
```python
from statsmodels.formula.api import logit

formula = "종속변수 ~ col1 + col2 + col3"
model = logit(formula,data = df).fit()
model.params
```

## 7. 카이제곱 검정
```python
from scipy.stats import chisquare

chisquare(f_obs = , f_exp= )
```


```python
from scipy.stats import chi2_contingency
chi2_congingency(obs)
```


